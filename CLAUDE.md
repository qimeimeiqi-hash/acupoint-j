# 2岁儿童穴位立体图 — 项目规格说明书

> 状态：**规划阶段，尚未开始开发**。本文件是需求确认后的规格说明书，作为后续开发的唯一依据。开发前请人工确认本文件内容无误。

---

## 0. 项目背景

用户（家长）拿到一张真实的**小儿推拿医嘱处方**（来源：桌面文件 `锦州穴位.jpg`），内容为：

> 2、推拿治疗，日一次，操作：
> (1) 头部：拿风池，拿肩井，按揉风池、脑户、脑空、哑门、太阳、百会、四神聪、人中，拿五经等；
> (2) 双上肢：补脾经、补肾经、揉板门，捏拿上肢，点按揉手三阴经、手三阳经腧穴，着重掐按十宣穴、内关穴；
> (3) 双下肢：捏拿下肢，按揉足三阴经、足三阳经腧穴，着重按揉足三里、三阴交、涌泉穴；
> (4) 捏脊。
> 3、1个月复诊，病情有变随诊。

用户是推拿新手，希望做一个网页工具，在人体图上标出这些穴位，点击后能看到「怎么找到这个穴位」+「用什么手法按摩」，帮助自己在家给2岁儿子做推拿时不出错、不遗漏。

**⚠️ 医疗免责声明（必须在网站首页和每个穴位详情页体现）**：
本工具仅用于辅助记忆处方中的穴位位置与手法，**不构成医疗建议，不能替代医生诊断**。具体推拿频次、力度、疗程请严格遵医嘱；若孩子出现不适、抗拒或病情变化，请及时复诊。

---

## 1. 需求确认结果（已与用户逐项确认）

| 决策项 | 结论 |
|---|---|
| 人体图呈现方式 | **2D 卡通插画**（非真 3D 模型），先做2D，未来有精力/素材再考虑升级3D |
| 人体图素材 | **通用卡通插画**，不使用孩子真实照片（因为免费 GitHub Pages 必须用公开仓库，网站公开可访问，用真实照片=公开孩子照片） |
| 讲解形式 | **先图文 + 简单方向动图/箭头**，不做视频；预留视频字段，未来可补充 |
| 穴位收录范围 | **只做处方里出现的穴位/手法点**，不扩展其他儿童保健穴位 |
| 技术栈 | **纯 HTML + CSS + JavaScript**，无需构建工具，直接部署 GitHub Pages |
| 引导模式 | **需要**：除自由点击查看外，提供「今天的推拿」按处方顺序逐步引导 + 打勾完成 |
| 内容详细度 | **完整版**：定位、手法（含方向）、时长/次数、作用、新手注意事项 |
| 离线支持 | **需要**：做成 PWA，可离线打开、可添加到主屏幕 |
| 部署目标 | GitHub Pages，100% 零成本 |

---

## 2. 内容清单：处方穴位/手法拆解

处方里的内容并不都是"单个穴位点"，有的是**经络循行路线**（一条线，带方向），有的是**手部特写才看得清的精细点**（脾经、板门、十宣等在手指/手掌上，正常大小的全身图根本标不下）。因此规划 **4 个视图**，而不是简单的正面/背面两张图：

- **正面图**（站立卡通小孩正面）
- **背面图**（站立卡通小孩背面）
- **手部特写图**（放大的手掌+手指，正面朝上）
- **足底特写图**（脚底朝向镜头）

### 完整穴位/手法清单（共 21 项）

| # | 名称 | 类型 | 所属视图 | 处方部位 |
|---|---|---|---|---|
| 1 | 风池 | 点 | 背面（颈后发际两侧） | 头部 |
| 2 | 肩井 | 点 | 背面（肩部） | 头部 |
| 3 | 脑户 | 点 | 背面（后脑正中） | 头部 |
| 4 | 脑空 | 点 | 背面（后脑两侧） | 头部 |
| 5 | 哑门 | 点 | 背面（后颈发际正中） | 头部 |
| 6 | 太阳 | 点 | 正面（侧头） | 头部 |
| 7 | 百会 | 点 | 正面（头顶正中） | 头部 |
| 8 | 四神聪 | 点（4个点） | 正面（头顶，百会周围） | 头部 |
| 9 | 人中 | 点 | 正面（唇上沟正中） | 头部 |
| 10 | 拿五经 | 手法/线 | 手部特写（五指掌面根部） | 头部 |
| 11 | 补脾经 | 点/线 | 手部特写（拇指桡侧面） | 上肢 |
| 12 | 补肾经 | 点/线 | 手部特写（小指掌面） | 上肢 |
| 13 | 揉板门 | 点 | 手部特写（大鱼际平面） | 上肢 |
| 14 | 手三阴经/手三阳经腧穴 | 线（经络路径） | 正面（手臂内外侧，带箭头） | 上肢 |
| 15 | 十宣穴 | 点（10个，指尖） | 手部特写（十指尖） | 上肢 |
| 16 | 内关穴 | 点 | 正面（前臂内侧腕横纹上2寸） | 上肢 |
| 17 | 足三阴经/足三阳经腧穴 | 线（经络路径） | 正面（小腿内外侧，带箭头） | 下肢 |
| 18 | 足三里 | 点 | 正面（小腿外侧） | 下肢 |
| 19 | 三阴交 | 点 | 正面（小腿内侧） | 下肢 |
| 20 | 涌泉穴 | 点 | 足底特写 | 下肢 |
| 21 | 捏脊 | 线（脊柱全线） | 背面（整条脊柱，带方向箭头） | 背部 |

> 开发时需要为每一项查证准确的定位描述、标准手法（推/揉/按/掐/捏/拿 + 方向）、参考次数/时长、功效、新手注意事项。这部分内容需要认真核实（可参考权威小儿推拿资料），因为直接影响孩子的实际护理效果，**不能编造**。

---

## 3. 功能需求

### 3.1 核心功能
1. **首页**：项目简介 + 医疗免责声明 + 两个入口按钮「自由探索穴位图」「开始今天的推拿（引导模式）」。
2. **人体图探索页**：
   - 视图切换：正面 / 背面 / 手部特写 / 足底特写（Tab 或按钮切换）。
   - 每个视图上用可点击的"热区圆点"标注穴位；经络路径用**带箭头的虚线/曲线**标注，点击线也能弹出说明。
   - 按处方部位（头部/上肢/下肢/背部）用不同颜色区分热区，附图例。
   - 点击穴位/路线 → 弹出详情卡片（见 3.2）。
3. **穴位详情卡片/页**（完整版内容）：
   - 名称（含如"补脾经"这类手法名）
   - 📍 定位：怎么找到这个位置（新手向文字描述，必要时配合图上高亮）
   - 🤚 手法：具体操作方式 + 方向（如"顺时针揉""从指根推向指尖"）
   - ⏱ 参考次数/时长
   - ✨ 作用：这个穴位/手法起什么效果
   - ⚠️ 新手注意事项：常见错误、力度提醒、孩子不配合怎么办
   - 🎬 讲解视频位：预留区域，显示"文字/动图讲解"，未来可插入视频（数据结构预留 `videoUrl` 字段）
4. **引导模式（"今天的推拿"）**：
   - 按处方顺序：头部 → 双上肢 → 双下肢 → 捏脊，逐条呈现。
   - 每条展示详情卡片内容，用户按摩完点击"完成，下一个"或打勾。
   - 顶部显示进度条/步骤计数（如 5/21）。
   - 全部完成后显示"今日完成！"，并可选**打卡记录**（见第6节可选功能）。
5. **搜索/筛选**（自由探索页）：按名称快速筛选穴位（考虑到21项不多，简单筛选即可，非必须但建议做）。

### 3.2 非功能需求
- **移动端优先**：实际使用场景是家长单手抱娃/单手操作手机，按钮要大、易点击，页面在手机浏览器上体验良好。
- **零成本**：不使用任何付费 API、付费素材、付费托管。所有图片素材需为免费可商用/自绘 SVG。
- **PWA 离线支持**：首次联网访问后可离线打开；支持"添加到主屏幕"。
- **无构建依赖**：纯静态 HTML/CSS/JS，浏览器直接运行，无需 Node 构建步骤，可直接用 GitHub Pages 从仓库根目录发布。
- **中文界面**，语气亲切、面向完全新手（避免使用未解释的专业术语）。
- **内容准确性优先**：穴位定位与手法描述需谨慎核实，宁可简单清楚，不可似是而非。

---

## 4. 设计方案

### 4.1 信息架构
```
index.html                  首页（简介+免责声明+两个入口）
explore.html                自由探索页（4视图切换 + 热区点击 + 详情弹层）
guide.html                  引导模式页（按处方顺序逐步走一遍）
/assets/
  /img/
    body-front.svg          正面插画
    body-back.svg           背面插画
    hand-closeup.svg        手部特写插画
    foot-sole.svg           足底特写插画
    icons/                  PWA 图标等
  /css/
    style.css
  /js/
    data.js                 穴位/手法数据（21项，含坐标、文案）
    app.js                  探索页交互逻辑（渲染热区、弹窗）
    guide.js                引导模式逻辑（步骤、进度、打卡）
    sw-register.js          Service Worker 注册
  manifest.webmanifest      PWA 配置
  service-worker.js         离线缓存
CLAUDE.md                   本规格说明书
README.md                   项目说明（给未来的自己看）
```

### 4.2 数据模型（`data.js` 中每条记录的字段设计）
```js
{
  id: "fengchi",
  name: "风池",
  category: "头部",          // 头部 | 上肢 | 下肢 | 背部
  type: "point",             // point | line
  view: "back",              // front | back | hand | foot
  position: { x: 42, y: 18 },   // 百分比坐标（point类型），响应式定位
  // line 类型改用 path: [{x,y}, {x,y}, ...] + directionArrow: true
  location: "在后颈部，枕骨下方，与耳垂齐平的凹陷处",
  technique: "拿法：用拇指与食指、中指相对用力，提拿两侧风池穴3~5次",
  duration: "3~5次，力度轻柔",
  benefit: "疏风解表，缓解风寒感冒、头痛、鼻塞",
  caution: "颈部力度要格外轻柔，孩子哭闹抗拒时不要强行按压",
  videoUrl: null,             // 预留，未来可填视频地址
  source: "处方原文：按揉风池"
}
```

### 4.3 视觉与交互
- 配色：柔和、儿童友好（推荐低饱和度的马卡龙色系），按 4 个部位分类配色 + 图例。
- 热区：圆点 + 序号或图标，点击有轻微放大/高亮反馈；移动端热区最小可点击尺寸 ≥ 40px。
- 经络路线：曲线路径 + 箭头，点击线上任意一段触发详情。
- 详情弹层：底部抽屉（bottom sheet）样式，适合手机单手操作，可上滑查看完整内容、点击外部关闭。
- 引导模式：类似"打卡清单"体验，步骤间有明显的"下一步"大按钮。

### 4.4 未来可选加分功能（本期不做，写入 backlog，供用户后续决定）
- 🔊 **语音朗读**（Web Speech API，浏览器原生，零成本）：推拿时双手不空，点击后自动朗读定位+手法文字，解放双手非常实用，建议作为**第一优先级的后续迭代**。
- 📅 打卡日历：用 `localStorage` 记录每天是否完成"今日推拿"，简单的连续打卡激励。
- 🎬 视频讲解：等用户自行拍摄手法演示短视频后接入。
- 🧍 真 3D 可旋转模型：需要找到合适的免费儿童体型模型并重新做坐标映射，工作量较大，暂不做。
- 🌐 更多穴位库：处方之外的儿童保健常用穴位，二期再扩展。

---

## 5. 技术方案

- **技术栈**：纯 HTML5 + CSS3 + 原生 JavaScript（ES6+），不引入任何前端框架，不需要 npm/构建步骤。
- **图形方案**：人体插画用 **SVG**（矢量，缩放不失真，方便叠加可点击热区，且体积小，适合零成本托管）。
- **PWA**：`manifest.webmanifest` + 简单的 `service-worker.js`（缓存所有静态资源，实现离线访问）。
- **数据存储**：穴位内容以静态 JS 对象数组硬编码在 `data.js` 中（不需要数据库/后端）。
- **托管与部署**：
  1. `git init` 本地仓库 → 推送到 GitHub 公开仓库。
  2. 仓库 Settings → Pages → 从 `main` 分支根目录发布（无需 GitHub Actions，因为没有构建步骤）。
  3. 访问地址形如 `https://<用户名>.github.io/acupoint-j/`。
- **测试**：Node.js 内置的 `node:test` + `node:assert`（零依赖，无需 `npm install` 任何包），命令行运行 `node --test`。仅测试纯逻辑/数据文件，不引入 jsdom 等第三方库模拟 DOM（详见第 8 节）。
- **兼容性**：目标是现代移动端浏览器（微信内置浏览器、Safari、Chrome），不需要兼容 IE。

---

## 6. 待办任务清单（Todo List）

> 以下清单中，凡涉及新增/修改 `.js` 逻辑文件的任务，均默认包含"编写/更新对应 `node:test` 测试文件"这一步，不再逐条重复列出，但**必须执行**（见第 8.3 节）。

### Phase 0：项目初始化
- [ ] `git init`，创建 `.gitignore`
- [ ] 搭建基础目录结构（如第 4.1 节）
- [ ] 创建 GitHub 仓库并推送初始代码
- [ ] 在仓库设置中开启 GitHub Pages，验证可访问

### Phase 1：内容数据核实与录入（最关键，直接影响推拿效果，需认真做）✅ 已完成
- [x] 逐项核实 21 个穴位/手法的准确定位描述（参考权威小儿推拿资料，来源见 `SOURCES.md`）
- [x] 逐项撰写手法说明（含方向、力度、次数/时长）
- [x] 逐项撰写功效说明（大白话，非专业术语堆砌）
- [x] 逐项撰写新手注意事项（常见错误、安全提醒）
- [x] 将以上内容整理进 `assets/js/data.js`，21 条记录全部补全
- [x] 编写 `assets/js/data.test.js`（node:test，14 项测试，覆盖结构校验+具体值断言+边界情况），全部通过

### Phase 2：视觉素材制作
- [ ] 确定/绘制正面卡通小孩插画（SVG）
- [ ] 确定/绘制背面插画（SVG）
- [ ] 绘制手部特写插画（SVG）
- [ ] 绘制足底特写插画（SVG）
- [ ] 确定配色方案与部位分类图例
- [ ] 为每个穴位标定插画上的坐标（point）或路径点（line）

### Phase 3：核心交互开发
- [ ] 首页 `index.html`（简介+免责声明+入口）
- [ ] 探索页：4 视图切换 Tab
- [ ] 探索页：热区渲染（point 类型圆点 + line 类型带箭头路径）
- [ ] 详情弹层组件（底部抽屉样式）
- [ ] 搜索/筛选穴位名称（可选但建议）
- [ ] 移动端适配与触控优化

### Phase 4：引导模式
- [ ] `guide.html` 按处方顺序的步骤流程
- [ ] 步骤进度条/计数
- [ ] "完成/下一步"交互
- [ ] 完成后的总结页面

### Phase 5：PWA 与离线
- [ ] 编写 `manifest.webmanifest`（图标、名称、主题色）
- [ ] 编写 `service-worker.js` 缓存策略
- [ ] 注册 Service Worker，测试离线打开
- [ ] 测试"添加到主屏幕"效果

### Phase 6：测试与验收
- [ ] 手机浏览器实测（微信/Safari/Chrome）
- [ ] 逐条核对 21 个穴位内容准确性（内容问题优先级最高）
- [ ] 检查免责声明是否醒目
- [ ] 检查所有热区在不同屏幕尺寸下点击是否准确
- [ ] Lighthouse 简单跑分（PWA/性能/可访问性）

### Phase 7：上线
- [ ] 最终检查 GitHub Pages 部署地址
- [ ] 写 `README.md` 说明项目用途与使用方法
- [ ] （可选）生成二维码方便手机直接扫码访问

---

## 7. 开放问题 / 需要用户后续决定

- [ ] 是否要在本期就加入"语音朗读"功能（技术上零成本，只是范围扩大）？
- [ ] 卡通插画风格偏好（简笔线稿 / 扁平色块 / 更可爱Q版）？需要时可再讨论或直接由开发者决定风格。
- [ ] GitHub 仓库名 / 最终 Pages 访问路径是否有偏好？

---

## 8. 开发规范与 AI 协作规则（必须严格遵守）

### 8.1 命名规范
- 所有变量、函数名称必须使用含义明确的英文（如 `getPointById`、`currentView`、`renderHotspots`），不允许拼音或无意义缩写（如 `xy`、`d1`）。
- 界面上展示给用户看的中文内容（穴位名称、定位/手法说明文字等）属于数据/文案内容，不受此规则约束，仍使用中文。

### 8.2 技术选型限制（禁止擅自替换）
- 严格按本文件第 5 节"技术方案"中指定的技术栈开发：原生 HTML + CSS + JavaScript（ES6+）+ Node.js 内置 `node:test`。
- **禁止擅自引入任何其他第三方库/框架/包**（包括但不限于 jQuery、React、Vue、Jest、Vitest、jsdom、Lodash、Day.js 等），无论多轻量。如果开发过程中确实认为需要额外依赖，必须先停下来向用户说明理由并取得确认，不能直接引入。

### 8.3 代码与测试同步
- 任何 `.js` 逻辑文件被新增或修改，都必须同步新增或更新对应的测试文件（约定命名：`xxx.js` ↔ `xxx.test.js`）。
- 为了能用零依赖的 `node:test` 覆盖测试（不引入 jsdom 等第三方库模拟浏览器 DOM），代码必须做到**纯逻辑与 DOM 渲染分离**：
  - 纯逻辑/数据处理函数——例如穴位数据校验、按 id/view/category 查找与过滤、引导模式的步骤与进度计算、经络路径点的坐标换算等——一律写成不直接读写 `document`/`window` 的独立函数，全部要有对应的 `node:test` 单元测试。
  - 只有真正涉及 DOM 渲染或事件绑定的代码（如 `renderHotspots()`、按钮的 `addEventListener`）可以不写自动化单元测试，但必须在 Phase 6 中做人工浏览器测试并记录测试结果。

### 8.4 测试代码质量要求（绝对严格遵守）
- 测试必须针对"具体输入 → 预期输出"进行验证，验证的是真实功能行为，不允许出现 `assert.ok(true)` 这类恒真的无意义断言。
- 严禁为了让测试通过而在测试代码里直接硬编码答案；严禁在正式代码中加入专门为测试服务的分支逻辑（如 `if (isTestMode) return ...`）。
- 开发流程必须先红后绿：先写出会失败的测试（运行 `node --test` 应报错/失败），再编写实现代码使其通过。
- 必须覆盖边界值、异常输入、错误场景，例如：查询不存在的穴位 id、坐标数值越界、引导模式在最后一步点击"下一步"时的行为、经络 line 类型缺少路径点时的处理等。
- 测试名称必须让人一眼看出在测什么，例如 `test('getPointById returns null when id does not exist')`，不能用 `test('test1')` 这类无意义命名。

### 8.5 需求不明确时不得假设
- 遇到本规格说明书未覆盖、或描述含糊的实现细节（例如某个穴位的精确坐标标准、测试覆盖的具体范围边界等），不允许自行假设后直接推进编码，必须先向用户提问确认。

---

**下一步**：请您审阅本文件，确认内容无误（尤其是第 2 节的穴位清单是否有遗漏或理解偏差，以及第 8 节的开发规范）。确认后再开始 Phase 0 的开发工作。
